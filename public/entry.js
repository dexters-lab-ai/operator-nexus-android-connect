// This is a placeholder entry point that will be replaced by the Vite build
console.log('Application entry point loaded');

// If we get here, the main app should handle the rest
if (window.startApp) {
  window.startApp();
}
